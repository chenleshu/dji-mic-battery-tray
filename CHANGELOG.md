# 更新记录

## 2.0.1 - 2026-09-02

- 修复首次查询尚未创建的 Windows 登录任务时，Task Scheduler 返回 `FileNotFoundException` 并导致自动启动状态检查失败的问题；任务不存在现在会正确显示为未启用，其他异常仍会报告。
- 自动启动从 `HKCU\Software\Microsoft\Windows\CurrentVersion\Run` 改为当前用户的 Windows 登录计划任务，托盘菜单同步更名为“登录后自动启动”。
- 安装位置改为 `%LOCALAPPDATA%\DjiMicBatteryTray\DjiMicBatteryTray.exe`，计划任务固定核对当前用户、登录触发、普通权限交互令牌、执行路径、`--autostart` 参数和工作目录。
- 安装器新增当前用户桌面快捷方式，并删除旧版同名 `Run` 启动值，避免 Windows 启动项批准状态与软件菜单显示不一致。
- 加强任务与卸载安全校验：只接受当前用户主体、唯一登录触发器和唯一程序动作；卸载器只删除根目录下且动作路径、参数和工作目录均属于本程序的任务，也不会删除无关的同名 `Run` 值。
- 旧启动项清理或日志写入失败不再阻断托盘主程序启动；菜单打开或用户修改设置后会重新读取真实任务状态。
- 安装完成后由计划任务受控启动程序，并在 30 秒内核对唯一进程的版本、进程 ID、执行路径、`autostart` 启动来源及任务启用状态；失败时安装器明确报错，不再把“任务已创建”等同于“启动链有效”。
- 当前 Windows 11 已完成上述受控任务启动链实机验证。此次没有注销或重启 Windows，因此没有把真实的下一次登录触发声明为已验证。

## 2.0.0 - 2026-09-01

- 将接收器按键接口 `USB\VID_2CA3&PID_4011&MI_00` 精确绑定到 WinUSB，在 Windows HID/Shell 收到音量命令之前完成源头拦截。
- 识别 `06-01-00` 按下与 `06-00-00` 松开，每次短按只触发一次目标组合。
- 新增常驻异步 WinUSB 中断读取器，动态发现 Interrupt-IN 端点，按物理接收器分别去重并支持热插拔和多个接收器。
- 删除 Raw Input、低级键盘钩子、Core Audio 音量恢复和时间相关性配对方案。
- 右 Alt 注入增加真实扫描码和 120 ms 保持时间，可直接切换 Typeless 2.5.0 听写启停。
- 新增 Typeless 识别完成后自动回车：只在停止听写后原输入框文字确实发生变化，并在最后一次变化后保持不变达到设定时间时提交；最长等待时间和文字停止变化后再等待时间均可设置。
- 自动读取 `MI_00` 设备注册的 WinUSB 接口 GUID，兼容由 Zadig/libwdi 为该子接口生成的安全驱动包；新增逐项核对与恢复说明，`MI_01` USB Audio 与 `MI_06` 电量接口保持独立。
- 保留 `右 Alt`、`右 Alt + Shift`、`右 Alt + 空格` 预设和最多 6 键自定义组合。
- 电量读取改为后台执行，托盘菜单与按键监听不再被 USB 读取阻塞。
- USB 发射器充电时显示闪电标识，悬停提示、设备详情和状态记录同步显示充电状态。
- 当前 Windows 11 已验证 `MI_00=WinUSB`、Interrupt-IN `0x82`、`MI_01=usbaudio`、`MI_06=WinUSB`；物理连接键可连续切换 Typeless 2.5.0，系统音量浮层不再出现，并已完成 2 个“开始—停止—文字补全—自动回车”实机周期；自动化测试 137 项通过。

## 1.6.0 - 2026-09-01

- 新增大疆连接键映射：使用 Raw Input 识别 `VID_2CA3&PID_4011` 来源，只转换大疆 HID 的“音量加”。
- 内置 `右 Alt`、`右 Alt + Shift`、`右 Alt + 空格` 三组一键预设。
- 新增自定义组合录制窗口，支持最多 6 个同时按下的键。
- 映射设置自动保存，默认启用并选择 `右 Alt`；托盘菜单可随时关闭。
- 普通键盘和其他设备的音量加事件原样回放；来源识别超时也保留系统音量，注入事件不会循环触发。

## 1.5.0 - 2026-09-01

- 读取并显示 USB 发射器报文中的真实充电状态。
- 任一 USB 麦克风充电时，托盘电池图标增加黄色闪电标识。
- 悬停提示在对应麦克风电量后显示闪电，右键详情同时显示“充电中”文字。
- 详情电池徽标为正在充电的麦克风增加黄色闪电，仍保留居中的百分比。
- 明确蓝牙模式只显示 Windows 已上报的电量，不猜测系统未提供的充电状态。

## 1.4.3 - 2026-08-31

- 设备详情和悬停提示移除“约”“约值”及波浪号。
- 电池徽标扩大为 68×26 像素，使用圆角轮廓和更清晰的文本渲染。
- 百分比始终居中显示在电池内部，改善小字号模糊和彩边。

## 1.4.2 - 2026-08-31

- 悬停提示为蓝牙和 USB 设备加入无线与插头符号。
- 右键详情页的蓝牙、USB 分组标题增加专用矢量图标。
- 在系统 63 字符限制内保留三支麦克风的接口、型号和电量。

## 1.4.1 - 2026-08-31

- 鼠标悬停托盘电池图标时，每支麦克风独立一行显示。
- 悬停提示只保留接口、设备型号和电量，不再显示序列号。
- 蓝牙电量显示精确百分比，USB 档位电量继续以“约”标识。

## 1.4.0 - 2026-08-31

- 设备详情改为按蓝牙接口和各 USB 接收器分组
- 每支麦克风独占一行，增加内含百分比的动态电池图标
- 解析 USB v2 身份帧中的接收器/发射器序列号和产品名称
- 可直接区分 DJI Mic Mini、DJI Mic Mini 2、DJI Mic Mini 2S 等设备上报型号
- USB 分组标题显示接收器产品类型、序列号和该组麦克风数量
- 身份字段不可用时显示“未识别”，不根据 TX 槽位猜测产品型号
- 当前三麦实机验证通过，自动化测试扩展至 35 项

## 1.3.0 - 2026-08-31

- 支持蓝牙与 USB 无线同时接入，不再采用蓝牙优先的单通道显示
- 支持同时连接多个蓝牙麦克风、多个 USB 接收器及其 TX1/TX2 发射器
- 托盘图标统一显示所有在线麦克风中的最低电量
- 右键“设备详情”逐项显示所有麦克风的电量和充电状态
- 蓝牙在线状态改为核对 Windows 免提录音端点；蓝牙掉线后不再显示缓存电量，并自动切换到 USB
- USB 多接收器并行读取，避免读取时间随设备数量线性增加

## 1.2.0 - 2026-08-31

- 支持 DJI Mic Mini 蓝牙 `Hands-Free` 连接
- 读取 Windows HFP 电池值并显示设备上报的精确百分比
- 蓝牙在线时优先显示蓝牙状态；未连接时自动使用原有 USB 读取链
- 蓝牙与 USB 均沿用绿色、橙色、红色电量视觉分级
- 已在一支 DJI Mic Mini 上完成连接、录音端点和电量显示实机验证
- 软件说明明确列出大疆蓝牙和 USB 无线连接两种模式，并增加托盘图标截图

## 1.1.0 - 2026-08-30

- 鼠标悬停显示 TX1/TX2 的估算百分比
- 估算低于 10% 时托盘图标变为橙色，5% 时变为红色
- 电池填充量同步作为粗略视觉区分
- 百分比明确标注“约”，避免与 DJI 未提供的精确百分比混淆

## 1.0.0 - 2026-08-29

- 首个 Windows EXE 发行版
- 应用、托盘菜单、提示文字和开机启动项统一命名为“大疆麦克风电量”
- 支持 DJI Mic Mini TX1/TX2 电量档位与充电状态
- 仅访问 Interface 6，不改变 USB Audio 接口
